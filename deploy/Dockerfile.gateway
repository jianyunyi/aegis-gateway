# AEGIS 网关运行时镜像（离线构建）：
# 二进制由宿主机交叉编译（make build-linux），此处只打包进 alpine 运行。
# 有网络环境时可用多阶段 golang 镜像构建（见 .github/workflows/ci.yml 说明）。
FROM alpine:3.20
RUN adduser -D -u 10001 app
COPY backend/bin/gateway-linux /usr/local/bin/gateway
USER app
EXPOSE 8080
ENTRYPOINT ["gateway"]
