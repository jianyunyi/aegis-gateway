# 面试讲稿与简历素材 — AEGIS

> 目的：把项目讲深、讲透。每个模块给出"30 秒电梯陈述 + 3 分钟深讲 + 追问预演"。
> 使用方式：对着本文件练习，直到能不看稿复述"核心链路"与"三大亮点"。

---

## 一、项目一句话定位（30 秒电梯陈述）

> **AEGIS 是一个企业级大模型网关（LLM Gateway）**：所有业务线通过它统一接入多家大模型，获得**鉴权、限流、计费、可观测、成本优化、模型评测**六大能力。我独立完成了从需求分析、架构设计到开发的完整闭环，目前已在本地 Docker 环境一键部署运行。

## 二、技术栈与分工（对应简历）

| 层    | 技术                          | 我的产出                         |
| ---- | --------------------------- | ---------------------------- |
| 网关核心 | Go + Gin                    | OpenAI 兼容代理、SSE 流式转发、中间件链    |
| 数据   | MySQL + Gorm / Redis        | 模型目录、调用日志、计费对账；令牌桶限流、缓存、预算计数 |
| 成本优化 | 自研路由/缓存/预算                  | 语义路由、请求缓存、月度预算降级             |
| 管理后台 | Next.js + React + TS + AntD | 大盘/Key/提供商/日志/账单/评测页面        |
| 工程化  | Docker + GitHub Actions     | 离线镜像构建、CI 流水线、压测             |

## 三、三大亮点（面试主菜，每个可讲 3 分钟）

### 亮点 1：OpenAI 兼容网关 + 流式架构（讲"为什么 Go"）

- 对外完全兼容 OpenAI 协议 → 任意官方 SDK 改 base_url 即接入，零改造
- 流式转发用**逐块透传（O(1) 内存）**而非全量缓冲 → 首字延迟（TTFT）被记录并入库
- 选型理由：SSE 是 IO 密集，Go 的 goroutine 模型在千路并发下内存优势明显；单二进制部署契合 Docker
- **数据支撑**：压测 200 并发下网关自身转发开销 P95=60ms、RPS≈5700，全链路 0 失败

### 亮点 2：计费一致性设计（讲"为什么不会多扣/漏扣"）

- **先扣后结 + 定时对账**（ADR-005）：Redis 计数做配额预检的快速路径，MySQL 流水是事实来源
- 每 5 分钟对账：发现 Redis 计数与 MySQL used_tokens 不一致时**以 MySQL 为准**纠正
- **演示过的场景**：人为把 Redis 计数改成 999，对账后自动纠正回 132
- 追问预演见下文"计费一致性"

### 亮点 3：成本优化三件套（讲"降本增效"）

- **语义路由**（ADR-006）：显式 Header > Key 默认模型 > 启发式选档（关键词/长度），先规则后 LLM——可解释、零额外成本，LLM 分类器作为扩展点
- **请求缓存**：相同请求直接命中，cost=0；缓存键含实际路由模型，避免跨模型错配（这是踩坑后修复的）
- **预算降级**：月度预算超限自动切到最便宜模型并告警
- **实测数据**：同质量分下 mini 模型成本仅为标准模型的 **28%**（评测飞轮验证）

## 四、追问预演（Top 20）

### 代理与流式

1. **为什么要兼容 OpenAI 协议？** 生态红利：任何 SDK 零改造接入；上游各家（DeepSeek/通义/豆包/Ollama）都兼容该协议，转换成本最低。
2. **流式怎么实现的？** 读上游响应逐行解析 SSE，写入客户端后立即 Flush；用 bufio.Scanner + 4MB 缓冲处理长行；客户端断开（用户取消）与上游错误分开记录。
3. **TTFT 怎么测的？** 记录从收到请求到写出第一行 SSE 的时间，入库 usage_logs.ttft_ms。
4. **上游超时怎么办？** 网关级 UpstreamTimeout 配置（默认 180s）；流式中断时向客户端发 SSE 错误并正确结束连接。
5. **如何支撑计费？** 流式请求自动注入 `stream_options.include_usage`，从结束块解析 usage。

### 鉴权与安全

6. **API Key 怎么存？** 只存 SHA-256 哈希 + 展示前缀（ADR-007），明文仅在创建时返回一次；泄露库也不泄露 Key。
7. **上游提供商 Key 怎么存？** AES-256-GCM 加密落库，密钥由网关 master secret 派生，代理调用时才解密。
8. **限流算法？** Redis Lua 原子脚本实现令牌桶（每 Key 可配 rps/burst），避免"读-改-写"竞态；Redis 不可用时 fail-open 保可用性（生产可切 fail-closed）。

### 计费一致性

9. **为什么用 Redis 计数而不用 MySQL？** 高并发扣减，Redis INCR 原子且快；MySQL 做流水与对账。
10. **对账怎么保证不丢？** 定时任务比较 Redis 计数与 MySQL used_tokens，不一致以 MySQL 为准重设；任务幂等（按 request_id 去重）。
11. **配额超了怎么办？** 预检返回 429 + 明确错误码（42902 quota_exceeded），并落一条失败日志。

### 成本优化

12. **启发式路由的准确率怎么保证？** 可解释、可离线测试；复杂意图通过 Header 显式指定或 Key 默认模型兜底；LLM 分类器作为扩展接口预留，等数据积累后再启用。
13. **缓存一致性？** 缓存键 = 实际路由模型 + 完整请求体，杜绝跨模型错配；TTL 可配（默认 600s）；命中时 cost=0 但 token 计入统计。
14. **预算降级会不会影响体验？** 超限后降级到最便宜模型而非拒绝服务，并打 WARN 告警；可在后台调高预算。

### 评测与观测

15. **评测怎么做的？** 真实调用日志采样（分层按模型）→ 人工打标 → A/B 回归：两模型分别回答全部样本，LLM-as-judge 打分（扩展点，可配 judge 模型），汇总质量分/成本/延迟报告。
16. **大盘有哪些指标？** 今日请求量/成本/Token/成功率/平均延迟 + 7/30 天趋势；调用日志全字段可查（TTFT、路由方式、缓存标记、错误码）。
17. **日志表数据量大怎么办？** 索引按 (api_key_id, created_at) 与 (model_name, created_at) 设计；billing_daily 预聚合表做归档（面试可引深：分表/归档策略）。

### 架构与工程

18. **为什么选 Go？** 流式代理 IO 密集场景 goroutine 优势；单二进制部署；与 Redis/MySQL 生态成熟。
19. **项目怎么保证质量？** 分层 handler→service→repository→model；统一错误码；评审 Checklist；Go 单测（routing/cache/budget/proxy）+ 前端 build 通过；压测报告记录 P95。
20. **部署方式？** Docker Compose 一键启动（网关/后台/MySQL/Redis/mock 沙箱）；离线镜像构建（宿主机交叉编译 + alpine 运行时）；CI 流水线 lint+test+build。

## 五、简历条目（可直接粘贴）

**企业级大模型网关 AEGIS（全栈，Go + Next.js）** · 个人独立项目

- 设计并实现 OpenAI 兼容的统一大模型接入网关：鉴权、限流、计费、观测、路由、评测六大模块，任意 OpenAI SDK 改 base_url 即接入
- 实现 SSE 流式逐块转发与 TTFT 首字延迟统计；Redis Lua 令牌桶限流（每 Key 可配）；200 并发压测网关开销 P95=60ms
- 设计"先扣后结 + 定时对账"计费体系（Redis 计数 + MySQL 流水 + 以 MySQL 为准的自动纠正），保证配额最终一致
- 实现语义路由（Header/规则/启发式三级）、请求缓存（含路由模型复合键）与月度预算自动降级，同质评测下成本降低 72%
- 搭建调用日志/大盘/日账单 API 与 Next.js 管理后台（登录、Key/提供商/模型管理、实时观测）
- 工程化：Docker Compose 一键部署（离线镜像）、GitHub Actions CI、压测工具与测试报告

## 六、演示脚本（5 分钟）

1. 打开管理后台（:3000）登录 admin → 大盘显示实时指标
2. 用 curl 演示流式对话（SSE 逐字输出）+ 无 Key 401
3. 演示语义路由：短问答走便宜模型（看日志 routed_by=heuristic）
4. 演示缓存：相同请求二次命中 cost=0（cached=1）
5. 演示对账：Redis 计数篡改后一键 reconcile 纠正
6. 演示评测：数据集 → 采样 → A/B 回归 → 报告（质量分 vs 成本）

## 七、诚实边界（面试被追问时主动说明）

- mock 上游为演示沙箱，评测分数当前是启发式兜底（LLM-as-judge 扩展点已预留）
- 限流组件在 Redis 故障时 fail-open（生产可切换 fail-closed，属配置决策）
- prompt_preview 仅截断未脱敏（企业版需接脱敏，风险登记册 R6 有记录）
- Windows 单进程压测受 TCP backlog（~200）限制，生产建议 Linux/反代（测试报告已记录）

## 八、面试必会清单（按被问概率排序）

> 原则：**先结论后细节、用数字说话、主动讲权衡**。每一项都要能"30 秒讲清 + 3 分钟深挖"。

### A 档：100% 会被问，答不出就崩（每题 3-5 分钟准备）

1. **为什么用 Go 做网关？** 流式代理是 IO 密集：goroutine 千路并发内存远优于 Node/Python；单二进制部署契合 Docker；Gin 中间件模型与网关中间件链一一对应。数据：压测 P95=60ms。
2. **SSE 流式怎么实现？为什么不用 WebSocket？** 逐块透传（bufio.Scanner + Flush，O(1) 内存）；SSE 单向服务端推送足够（LLM 是服务端→客户端），HTTP 兼容性好、自动重连、无需协议升级。
3. **令牌桶限流原理 + 为什么用 Lua 脚本？** 桶容量 burst + 每秒补充 rate；Redis 原子执行避免"读-改-写"竞态；每 Key 可配。
4. **计费怎么保证不重不漏（最终一致性）？** 先扣后结：Redis 计数做快速预检，MySQL 流水是事实；定时对账以 MySQL 为准纠正 Redis（演示过 999→132）。
5. **API Key 安全怎么设计？** 只存 SHA-256 哈希 + 前缀展示，明文仅创建时返回一次；提供商 Key AES-256-GCM 加密，master secret 派生密钥。
6. **语义路由怎么做的？** 三级：Header 显式 > Key 默认模型（规则）> 启发式（关键词/长度选档）；先规则后 LLM——可解释、零成本，LLM 分类器是扩展点。
7. **缓存怎么避免脏数据？** 缓存键 = 实际路由模型名 + 完整请求体（复合键），杜绝跨模型错配（这是踩坑修复的点）；TTL 可配。
8. **MySQL 大数据量表怎么查得快？** usage_logs 按 (api_key_id, created_at)、(model_name, created_at) 联合索引；日账单走预聚合（billing_daily）。
9. **项目怎么保证质量？** 分层架构 + 统一错误码 + 评审 Checklist + 单测（routing/cache/budget/proxy）+ CI（vet/test/build）+ 压测报告。

### B 档：大概率被深挖（每题 2-3 分钟）

10. **Redis 数据结构用了哪些？** String（限流计数/配额/预算/缓存）、Lua 脚本（原子限流）；为什么不用 ZSET/List 等（场景差异）。
11. **HTTP 401/429/502 的错误码设计？** 代理侧 OpenAI 格式 `{"error":{message,type}}`；管理侧 `{code,data,message}` + 业务错误码表（40001/40101/42902…）。
12. **无状态网关怎么水平扩容？** 状态全在 Redis/MySQL，网关只做转发+计算；加实例即扩容；上游故障靠多提供商 priority 切换。
13. **预算降级怎么不伤体验？** 超限降级到最便宜模型而非拒绝服务 + WARN 告警；后台可调。
14. **评测飞轮怎么闭环？** 真实采样（分层按模型）→ 人工打标 → A/B 回归 → LLM-as-judge 打分（扩展点）→ 质量分/成本/延迟报告。
15. **上下文（context）怎么用？** 上游调用携带 `c.Request.Context()`，客户端断开即取消上游请求（省 token）。
16. **Gorm 的坑？** MySQL 9 only_full_group_by 导致聚合 SQL 报错（GROUP BY 与 SELECT 表达式不一致）——改按同一表达式分组。
17. **Docker 镜像怎么构建的？** 离线方案：宿主机交叉编译 + alpine 运行时（避免构建拉 golang 镜像）；前端 node_modules 含平台二进制必须容器内 npm ci（踩过 Windows→Linux 的坑）。
18. **压测怎么做的？** 自写 loadtest 工具：并发 worker 池 + 延迟分位数统计；发现 Windows backlog(~200) 限制 → 生产建议 Linux/反代。

### C 档：加分区分度（能讲就讲）

19. **LLM-as-judge vs 传统评估？** 启发式兜底 + judge 模型扩展点；评测分数与成本、延迟构成决策矩阵（面试亮点）。
20. **风险登记册 / ADR / 变更管理**：章程里有风险表（预算超支/范围蔓延），架构决策全部 ADR 化（ADR-001~007）——体现工程管理意识。
21. **安全纵深**：CORS 配置、输入校验、日志不落 Key、生产检查清单（JWT_SECRET/禁用种子/HTTPS）。
22. **为什么网关不做 RAG/向量库？** 范围管理（Out-of-scope 明确写出）——防蔓延，面试官问"为什么不做 X"时展示判断力。

### 应急话术（被问住时的 3 个万能句式）

- "我们当时在 X 和 Y 之间权衡，最终选 X 是因为……（讲约束）"
- "这个点我们的设计是这样，但如果在生产环境，我会补……（讲边界）"
- "这块我踩过坑：……（讲具体教训）"
